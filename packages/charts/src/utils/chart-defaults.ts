/** Default chart height in dp */
export const CHART_HEIGHT = 220;

/** Default inner padding applied to every chart */
export const CHART_PADDING = { left: 8, right: 8, top: 12, bottom: 0 } as const;

/** Default domain padding so bars / points don't touch the axes */
export const DOMAIN_PADDING = { left: 20, right: 20, top: 16 } as const;

/** Default bar corner rounding */
export const BAR_RADIUS = 4;

/** Default line stroke width */
export const LINE_STROKE_WIDTH = 2.5;

/** Default area fill opacity */
export const AREA_OPACITY = 0.18;

/** Donut inner-radius fraction of the outer radius (0–1) */
export const DONUT_INNER_RADIUS = 0.58;
